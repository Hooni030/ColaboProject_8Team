using UnityEngine;

public class TestScripts : MonoBehaviour
{
    // 주석 테스트 한글 테스트
    // 가나다라마바사
    
    private string test = "Test, 테스트";
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
